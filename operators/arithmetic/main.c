#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
   printf("enter a number:\n");
   scanf("%d",&a);
   printf("enter a number:\n");
    scanf("%d",&b);
    printf("sum is=%d",a+b);
    printf("sub is=%d",a-b);
    printf("mul is=%d",a*b);
 return 0;
}
