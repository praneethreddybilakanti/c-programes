#include <stdio.h>
#include <stdlib.h>

int main()
{
printf("\n char of %d and its pointer ;%d",sizeof(char),sizeof(char*));
printf("\n int of %d and its pointer ;%d",sizeof(int),sizeof(int*));
printf("\n float of %d and its pointer ;%d",sizeof(float),sizeof(float*));




    return 0;
}
