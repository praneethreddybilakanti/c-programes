#include <stdio.h>
#include <stdlib.h>

int main()
{
int num;
printf(" enter any number :\n");
scanf("%d",&num);
if(num<10)
{

    printf("the given number is :%d less than 10.",num);
}
    return 0;
}
